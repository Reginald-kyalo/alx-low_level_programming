#include "main.h"

/**
 * _isdigit - checks number 0 through 9
 *
 * @c: to be checked
 * Return: 0 failure
 */

int _isdigit(int c)
{
	if (c >= 0 && c <= 0')
	{
		return (1);
	}
	else
	{
		return (0);
	}

}
